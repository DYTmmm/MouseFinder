#include <cstdio>

// ─── Mouse Finder 测试入口 ─────────────────────────────────────
// 后续各任务的属性测试和单元测试将以独立函数形式添加到此文件
// 或在 test/ 目录下新建 test_*.cpp 并加入 CMakeLists.txt
//
// 当前为工程搭建阶段占位，仅验证控制台目标可正确编译并运行。

int main()
{
    printf("All tests passed\n");
    return 0;
}
